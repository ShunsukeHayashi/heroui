import React from "react";

export const ElementPropertiesPanel: React.FC = () => {
  // TODO: Fix type errors - selectedElement and setSelectedElement don't exist in ProjectContextType
  // This component needs to be properly integrated with the ProjectContext or refactored
  return (
    <div className="h-full flex flex-col items-center justify-center p-4">
      <p className="text-default-500 text-center">Properties panel coming soon</p>
    </div>
  );
};